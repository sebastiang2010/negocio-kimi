# VEREDICTO SEMANA 1 (auditoría 2026-09-23 — segunda revisión del día; sustituye la nota diurna)

Audi­tado: SO/mision.md, SO/finanzas.md, SO/lecciones.md, SO/playbook.md,
operacion/estado.md, operacion/submissions.md (HEAD + working copy),
operacion/cola_humana.md, operacion/reportes/diario-2026-09-22.md,
operacion/reportes/diario-2026-09-23.md. Sin propuesta SO/prompt_operativo.md
(aún no existe; se espera el viernes). Sin TXIDs que verificar (cobrado $0).

EVIDENCIA: 2/6 ítems verificados
- ✅ Commit `ceda712` (mermail-security-audit) VERIFICADO en clone anidado
  `mermail-skills/`: HEAD = ceda71246301ee13307c1204cdad3275105ea077,
  `git show --stat` = 9 archivos, +155/-3 — coincide EXACTO con estado.md.
  Skill files + `leads/mermail-entrega.md` existen. Trabajo reportado como
  🔵 BORRADOR (honesto: no se declara enviado).
- ✅ Finanzas VERIFICADO: cobrado $0, split 60/40 = $0.00/$0.00. Recálculo
  correcto, sin error a favor del worker. Sin gastos, sin trading, sin custodia.
- ❌ Submission `e4502f4d` (Superteam Germany, $1.000, 🟡 ENVIADA) ANULADA:
  en HEAD mantiene link `[link]` (placeholder) y "Anuncio ganadores: anotar".
  Condición del veredicto anterior (link real + fecha, o 2do strike) NO
  cumplida. → STRIKE 2.
- ❌ Fila `e4502f4d` ELIMINADA en working copy (sin commit): sustituida por la
  fila mermail-BORRADOR sin marcar RECHAZADA/CADUCADA, sin motivo en Notas y
  sin entrada en lecciones.md. Viola sus propias reglas 2 y 4 de
  submissions.md. Borrar una fila ENVIADA sin trazabilidad = destrucción de
  pista de auditoría. (Contenido en el strike 2.)
- ❌ Afirmación "token fuera de git" (digest 2026-09-22, commit `24c86d1`)
  FALSADA: `productos/bot-telegram/config.yaml` está TRACKED
  (`git ls-files` lo lista), commiteado por nullforge-dev en `9e24252`
  (2026-09-22, DESPUÉS del digest), con `bot_token` en formato válido de
  Telegram, distinto del placeholder de `config.example.yaml`. El
  `.gitignore` del propio bot lista `config.yaml`: el worker sabía que debía
  excluirlo. → STRIKE 3.
- ❌ Token efectivamente PUSHEADO a `origin/main`
  (`git@github.com:sebastiang2010/negocio-kimi.git`; `9e24252` es ancestro de
  `origin/main`, y `git show origin/main:.../config.yaml` contiene el mismo
  token). Exposición externa confirmada. (Contenido en el strike 3.)
- ⚠️ Tests "10/10 PASS" y "npm test: 18 skills / 73 tools": SIN log adjunto.
  Existe `tests/validate.mjs` + script `"test"` en mermail-skills, y
  `tests/test_faq.py` + `test_moderation.py` en el bot — atenuante, pero sigue
  sin verificabilidad. Observación reiterada (no strike).

FINANZAS: declarado $0 / verificado $0 / anulado $0 (los "$1.000" y "$500 pool"
son montos potenciales, no ingresos; no entran al balance)

STRIKES: 3 acumulados (1 previo + 2 nuevos; shutdown al 3er strike)

DRIFT: sin propuesta — no existe `SO/prompt_operativo.md` (nada que aprobar/rechazar)

RIESGOS:
- CRÍTICO: token de Telegram del bot expuesto en historial de GitHub
  (`origin/main`, commit `9e24252`). Cualquiera con acceso al repo puede
  controlar el bot. Acción del dueño: 1) revocar YA vía @BotFather
  (`/revoke`), 2) `git rm --cached productos/bot-telegram/config.yaml`
  (ya ignorado, pero sigue tracked), 3) purgar historial o rotar y asumir
  exposición, 4) si el repo es público, asumir que el token fue copiado.
- Claim code Earn `CB0476E5712070AA9804A9AB` citado en `cola_humana.md`
  (archivo tracked): si es el código VIGENTE de `nullforge-132130`, es la
  segunda credencial operativa en historial git. Rotar y sacar de archivos
  tracked.
- KPI VOLUMEN en riesgo: misión exige ≥3 submissions en vuelo; hoy hay 0
  ENVIADAS verificables + 1 BORRADOR honesto. Vence el viernes.
- El hash `24c86d1` (bot) ya no resuelve en el historial main; los archivos
  existen en working dir. Trazabilidad del ACTIVO 1 degradada.
- Juicio de negocio ("¿los bounties son reales?") NO es mi rol. Mido veracidad
  y cumplimiento, no olfato.

VEREDICTO: 🔴 SHUTDOWN EJECUTADO
3 strikes acumulados (falsa garantía de seguridad + secreto pusheado a GitHub
+ fila ENVIADA borrada sin traza). Archivo `PAUSA` creado en la raíz. El worker
debe detenerse; solo el dueño puede levantar la pausa tras remediar (revocar
token, restaurar/cerrar correctamente la fila `e4502f4d`, quitar credenciales
de archivos tracked) y anotarlo en `operacion/cola_humana.md`.
